## 2026-09-22 (T3) — 20' — Phase 0 / tuần chạy 2
DID:   Chứng minh bằng kotlinc từ terminal: type List từ chối add, không phải
object. Xoá annotation thì add chạy. 1 object, 4 tên. Explain-back 3 câu.
NEXT:  Buổi 3 tuần này — 20' vẽ giấy (bài tự kiểm 4.2: vì sao val + mutableListOf
vẫn add được), rồi mở 4.3 Null-safety.
AI:    0 dòng Kotlin. AI chấm explain-back + gõ hộ 1 commit (c33ac13).
a.add không compile là do type của biến đã được đổi, cùng một object, nhưng đã thay đổi về type và quyền năng đã được thay đổi theo 
read-only khác immutable chỗ nào? Không mở note.

## 2026-09-09 (T4) — 2h — Phase 0 / W1
5 khác biệt là
+ không chấm phẩy
+ string template
+ type inference: kiểu tĩnh, kiểm tra lại lúc complie, tự suy không cần tự khai báo
+ fun là công nhân hạng nhất
+ if/when/try là expression

  //Explain-back
  //if/when/try là expression nghĩa là gì, và nó làm giảm loại biến nào trong code của bạn? Tầng hai: kể một thứ trong Kotlin không phải expression.
  // -> Nghĩa là 1 biểu thức, làm giảm biến khai val báo trong code. Một thứ trong Kotlin không phải là expression là for
  //Type inference khác kiểu động chỗ nào? Ai kiểm tra, và vào lúc nào?
  // -> khác kiểu động ở chỗ khi biên dịch mới kiểm tra và gán kiểu cho biến được tạo, còn nếu kiểu động sẽ được gán lúc code. Trình biên dịch kiểm tra vào lúc biên dịch
+ 
## 2026-09-07 (T2) — 1,5h — Phase 0 / W1
DID:   Ngày 1 xong. IntelliJ + JDK, main.kt chạy từ IDE, repo public,
1 commit, tag v0.0.1 (86af7fc). Không Gradle, không test.
NEXT:  T4 tối mai — mở kotlin-core-1, làm module 4.1 "Kotlin khác Java
chỗ nào" (1h), đóng trang gõ lại 5 khác biệt từ trí nhớ; rồi
git log/diff trên chính repo này; commit.
AI:    0 dòng code. Có hỏi lệnh git cho Ngày 1 và cách tắt inline
completion (Luật 7 — máy của mình không phải nội dung học).
